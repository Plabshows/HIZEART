---
const enquiryTypes = [
  "Available artwork",
  "Mural commission",
  "Brand collaboration",
  "Exhibition",
  "VR Art project",
  "Press / Gallery",
  "General enquiry"
];

const formspreeEndpoint = "https://formspree.io/f/mzdwezqn";
---

<form
  class="contact-form"
  name="hize-contact"
  action={formspreeEndpoint}
  method="POST"
  data-form-provider="formspree"
>
  <input type="hidden" name="_subject" value="New enquiry from HIZE ART website" />
  <p class="hidden-field">
    <label>Do not fill this out <input name="_gotcha" tabindex="-1" autocomplete="off" /></label>
  </p>
  <label>
    <span>Name</span>
    <input type="text" name="name" autocomplete="name" required />
  </label>
  <label>
    <span>Email</span>
    <input type="email" name="email" autocomplete="email" required />
  </label>
  <label>
    <span>Type of enquiry</span>
    <select name="type" required>
      <option value="">Select an option</option>
      {enquiryTypes.map((type) => <option value={type}>{type}</option>)}
    </select>
  </label>
  <label>
    <span>Message</span>
    <textarea name="message" rows="7" required></textarea>
  </label>
  <button class="button button--primary" type="submit">Send Enquiry</button>
  <p class="form-fallback">
    Secure form powered by Formspree. Direct email fallback:
    <a href="mailto:info@hizeart.com">info@hizeart.com</a>
  </p>
</form>
